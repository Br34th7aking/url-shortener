[tool.ruff]
# Migrations are autogenerated artifacts — don't lint or format them.
extend-exclude = ["**/migrations/**"]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings.dev"
python_files = ["tests.py", "test_*.py", "*_test.py"]
